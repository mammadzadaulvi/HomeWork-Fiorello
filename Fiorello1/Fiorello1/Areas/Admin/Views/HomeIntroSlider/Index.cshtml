@using Fiorello1.Areas.Admin.ViewModels.HomeIntroSlider
@model HomeIntroSliderIndexViewModel


<div class="main-panel">
    <div class="content-wrapper">
        <div class="row">
            <div class="col-lg-12 grid-margin stretch-card">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title">Home Main Slider</h4>
                        @if (Model.HomeIntroSlider != null)
                        {
                            <div class="table-responsive">
                                <table class="table">
                                    <thead>
                                        <tr>
                                            <th>Id</th>
                                            <th>Title</th>
                                            <th>Description</th>
                                            <th>AddPhoto</th>
                                            <th>Actions</th>
                                        </tr>
                                    </thead>
                                    <tbody>

                                        <tr>
                                            <td>@Model.HomeIntroSlider.Id</td>
                                            <td>@Model.HomeIntroSlider.Title</td>
                                            <td>@Html.Raw(Model.HomeIntroSlider.Description)</td>
                                            <td><img src="~/assets/img/@Model.HomeIntroSlider.AddPhotoName" /></td>

                                            <td>

                                                <a class="btn btn-primary " asp-action="update" asp-controller="homeintroslider" asp-area="admin" asp-route-id=@Model.HomeIntroSlider.Id>Update</a>
                                                <a class="btn btn-primary btn-danger " onclick="confirm('eminsinizmi?')" asp-action="delete" asp-controller="homeintroslider" asp-area="admin" asp-route-id=@Model.HomeIntroSlider.Id>Delete</a>
                                                <a class="btn btn-primary btn-details " asp-action="details" asp-controller="homeintroslider" asp-area="admin" asp-route-id=@Model.HomeIntroSlider.Id>Details</a>
                                            </td>
                                        </tr>

                                    </tbody>
                                </table>
                            </div>
                        }
                        else
                        {
                            <a class="btn btn-primary" asp-action="create" asp-controller="homeintroslider" asp-area="admin">CREATE HomeMain Slider!</a>

                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>